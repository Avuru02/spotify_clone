import { ProductWithPrice } from "@/types";

const getActiveProductsWithPrices = async (): Promise<ProductWithPrice[]> => {
  // Demo mode: no products/subscriptions
  return [];
}

export default getActiveProductsWithPrices;
